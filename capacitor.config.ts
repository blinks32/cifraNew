import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ciflex.app',
  appName: 'Ciflex',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
